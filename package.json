{
  "name": "codex-chunked",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "codex-chunked": "dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && pnpm link --global",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@openai/codex-sdk": "0.118.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
