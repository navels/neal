# Sandbox Backlog

Use this file only as a local sandbox backlog when you want a safe in-repo validation target.

- [ ] Improve `toSentenceCase()` so it preserves common all-caps acronyms like `CLI`, `API`, and `SDK`.
- [ ] Add a `compactPath()` helper to shorten long slash-delimited paths for display.
- [ ] Add a `buildReviewHeadline()` helper that formats a short status line from a title, finding count, and state.
- [ ] Add usage examples for the exported sandbox helpers in `notes/testing/SANDBOX_USAGE.md`.
- [ ] Export every sandbox helper from `src/sandbox-helpers/index.ts` with brief file-level comments where helpful.
